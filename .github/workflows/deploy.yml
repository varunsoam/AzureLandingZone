name: Deploy Two Resource Groups

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      runCheckov:
        description: 'Run Checkov Scan'
        required: true
        type: boolean
        default: true

permissions:
  id-token: write
  contents: read

jobs:
  terraform-deploy:
    # GitHub Reusable workflows don't support 'env' context in the 'with' block.
    # We will pass values directly or use 'vars'/'secrets'.
    uses: varunsoam/pipeline-template/.github/workflows/terraform-pipeline.yml@main
    with:
      workingDirectory: '.'              # Hardcoded or use ${{ vars.WORKING_DIRECTORY }}
      resourceGroupName: ${{ vars.BACKEND_RG }}
      storageAccountName: ${{ vars.BACKEND_STORAGE }}
      containerName: ${{ vars.BACKEND_CONTAINER }}   # Using GitHub Variables
      backendKey: 'rg-deployment.tfstate'
      environment: 'prod'
      terraformVersion: '1.11.0'
      runCheckov: ${{ fromJson(github.event.inputs.runCheckov || 'true') }}
    secrets: inherit
